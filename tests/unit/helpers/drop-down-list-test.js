import { dropDownList } from 'swimmingly/helpers/drop-down-list';
import { module, test } from 'qunit';

module('Unit | Helper | drop down list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dropDownList([42]);
  assert.ok(result);
});
